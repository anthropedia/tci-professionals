{
  "name": "tci-professionals",
  "version": "0.1.0",
  "description": "TCI for professionals",
  "main": "index.html",
  "scripts": {
    "start": "concurrently 'npm run build -- -w' 'live-server --no-browser'",
    "watch": " -w",
    "build": "lego bricks/ dist/bricks.js",
    "deploy": "ssh athens 'cd tci/professionals && git pull && npm install && npm run build'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "https://gitlab.com/anthropedia/tci/professionals"
  },
  "author": "Vincent Agnano",
  "license": "ISC",
  "dependencies": {
    "@polight/lego": "^0.7.2",
    "jsdom": "*"
  },
  "devDependencies": {
    "concurrently": "*",
    "live-server": "*"
  }
}
